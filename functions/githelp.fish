function githelp -d "My package helping for Git"
  # Package entry-point
  echo "githelp usage"
  echo "gitback : return to the previous branch"
  echo "gitbacklist : show the list of the previous branches"
  echo "gitquickcommit: git add + commit + push"
end

